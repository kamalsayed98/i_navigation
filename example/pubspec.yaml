name: example
description: A new Flutter project to test INavigation package.

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  i_navigation: 
    path: ../../i_navigation


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true